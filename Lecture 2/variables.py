# Variabler
a = "tjenare"
b = 1
print(a, b)

# ctrl+alt+R to run the script

# Variable name cannot start with a number eg. "1app"
# Can contain numbers, just not start with them
# No spaces in variable names
# If you name a variable the same name as a function, that function wil no longer work
